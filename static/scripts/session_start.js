document.getElementById('session-start-form').addEventListener('submit', function (event) {
    event.preventDefault();
    
    // Set session name and process PDF upload
    
    // Navigate to index.html
    window.location.href = '/interaction';
});